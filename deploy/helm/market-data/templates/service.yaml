apiVersion: v1
kind: Service
metadata:
  name: {{ include "market-data.fullname" . }}
  labels:
{{ include "market-data.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
{{ include "market-data.selectorLabels" . | indent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
