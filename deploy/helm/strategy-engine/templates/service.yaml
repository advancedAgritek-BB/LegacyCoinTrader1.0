apiVersion: v1
kind: Service
metadata:
  name: {{ include "strategy-engine.fullname" . }}
  labels:
{{ include "strategy-engine.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
{{ include "strategy-engine.selectorLabels" . | indent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
